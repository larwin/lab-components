import { OVERSCAN_DEFAULT } from '@/core/collection/virtual/constants';

export { OVERSCAN_DEFAULT };

/** Fallback container height when none is measured yet. */
export const DEFAULT_CONTAINER_HEIGHT = 400;
