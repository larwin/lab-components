import { LIST_OVERSCAN_DEFAULT } from '@/core/collection/list/constants';

/** Default List container height when `height` is not provided. */
export const LIST_DEFAULT_HEIGHT = 400;

/** UI re-export to avoid direct core import from List consumers. */
export { LIST_OVERSCAN_DEFAULT };



