import { OVERSCAN_DEFAULT } from '@/core/collection/virtual/constants';

/** Default item height when no policy is provided. */
export const LIST_DEFAULT_ITEM_HEIGHT = 36;

/** Re-export for consumers who use the List-prefixed name. */
export { OVERSCAN_DEFAULT as LIST_OVERSCAN_DEFAULT };

